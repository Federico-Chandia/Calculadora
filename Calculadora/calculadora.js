const sumar = require('./sumar');
const restar = require('./restar')
const multiplicar = require('./multiplicar');
const dividir = require('./dividir');
const raiz = require('./raizCuadrada');
const potencia = require('./potencia');

module.exports = {
    sumar,
    restar,
    dividir,
    multiplicar,
    raiz,
    potencia,
}